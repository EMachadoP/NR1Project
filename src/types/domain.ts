export type UserRole = "admin" | "hr" | "manager";

export type CampaignStatus = "draft" | "scheduled" | "active" | "closed" | "archived";

export type QuestionnaireStatus = "draft" | "published" | "archived";

export type TokenStatus = "available" | "used" | "expired" | "revoked";

export type ActionPlanStatus = "open" | "in_progress" | "done" | "cancelled";
